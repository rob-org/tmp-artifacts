name: Artifacts

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world" > hello.txt
      - uses: actions/upload-artifact@v3
        with:
          name: foobar
          path: hello.txt
